# SkillFather has zero external runtime dependencies — it uses only the Python
# standard library.  This file exists for tooling compatibility only.
#
# Development dependencies are declared in pyproject.toml under [project.optional-dependencies].
# Install them with:  pip install -e ".[dev]"
